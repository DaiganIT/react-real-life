import React from 'react';
import FirstExample from './first';

export default class SearchBoxExample extends React.Component {
  render() {
    return (
      <React.Fragment>
        <FirstExample />
      </React.Fragment>
    );
  }
}
